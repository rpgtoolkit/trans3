class Test
{
public:
    var file;
    var path;
    var type;
    
private:
    
    method myData(string)
    {
        attackAll();
        global(testing);
        local(tested); //Add local variables back to autcomplete.
        
        testing = 100;
    }
   
}